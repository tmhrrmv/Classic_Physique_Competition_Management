[phases.setup]
nixPkgs = ["php84", "php84Extensions.pdo", "php84Extensions.pdo_mysql", "php84Extensions.mbstring", "php84Extensions.curl", "php84Extensions.openssl"]

[start]
cmd = "php -S 0.0.0.0:$PORT"
